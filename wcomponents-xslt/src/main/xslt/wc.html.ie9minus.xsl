<xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
	xmlns:ui="https://github.com/bordertech/wcomponents/namespace/ui/v1.0"
	xmlns:html="http://www.w3.org/1999/xhtml"
	version="2.0">
	<!-- 
		This is a placeholder template for theme overrides if your theme NEEDS CSS for IE9 or earlier where conditional comments are supported. 

		This is because IE 8 and before (and sometimes IE9) are **really** bad at loading CSS in JavaScript.
	-->
	<xsl:template name="addOldIECSS" />
</xsl:stylesheet>